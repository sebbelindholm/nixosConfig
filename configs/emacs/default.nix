{ pkgs, ... }:

{
    /*
    programs = {
        doom-emacs = {
            enable = false;
            doomPrivateDir = ../configs/doom;
        };
    };

    services = {
        emacs = {
            enable = true;
        };
    };
    */
}