git
git init --bare $HOME/.setup
alias config='/usr/bin/git --git-dir=$HOME/.setup/ --work-tree=$HOME'\

config config --local status.showUntrackedFiles no\

echo "alias config='/usr/bin/git --git-dir=$HOME/.setup/ --work-tree=$HOME'" >> $HOME/.bashrc\

git status
config status
touch .setup/git_checkout.sh
config add scripts
config status
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew -v
brew -v
brew install
quit
exit
brew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
    (echo; echo 'eval "$(/opt/homebrew/bin/brew shellenv)"') >> /Users/sebastian/.zprofile
    eval "$(/opt/homebrew/bin/brew shellenv)"
brew -v
config add .zprofile
config status
config
touch .zshrc
echo 'alias config='/usr/bin/git --git-dir=/Users/sebastian/.setup/ --work-tree=/Users/sebastian' >> .zshrc\
'
config
config add .zshrc .zprofile
config add .zsh_history
config status
brew tap homebrew/cask-fonts
brew install font-fira-mono-nerd-font
brew install starship
brew
clear
starship
clear
neofetch
neofetch
clear
ls
ls -al
mkdir .ssh
cd .ssh
ls -al
cd ..
ls -al
sudo chmod 700 .ssh
sudo chmod 600 .ssh/id_rsa
sudo chmod 600 .ssh/id_rsa.pub
brew install --cask visual-studio-code
zsh <(curl -s https://raw.githubusercontent.com/zap-zsh/zap/master/install.zsh) --branch release-v1\

brew install --cask discord
zap update all
zap update all
curl -sL --proto-redir -all,https https://raw.githubusercontent.com/zplug/installer/master/installer.zsh | zsh\

zap -v
plug
zap
zplug
brew install zap
brew install --cask zap
plug
zap
zap -v
touch .gitconfig
touch .global_gitignore
config add .gitconfig .global_gitignore
brew install eza
eza
ls
ls -al
clear
